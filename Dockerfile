# First specify that we need the slim node image
FROM node:slim

# And then create a container that can be used to serve the webpages. Till then
# do nothing

# Future commands to keep in mind:
# npm install -g react-tools webpack
